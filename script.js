const header=document.querySelector('[data-header]');const toggle=document.querySelector('[data-menu-toggle]');const nav=document.querySelector('[data-nav]');const images=[...document.querySelectorAll('.hero-gallery img')];const motion=document.querySelector('[data-motion-toggle]');let slide=0,paused=false;const updateHeader=()=>header?.classList.toggle('is-scrolled',scrollY>18);const openNav=open=>{toggle?.setAttribute('aria-expanded',String(open));nav?.classList.toggle('is-open',open);header?.classList.toggle('is-open',open)};addEventListener('scroll',updateHeader,{passive:true});updateHeader();toggle?.addEventListener('click',()=>openNav(toggle.getAttribute('aria-expanded')!=='true'));nav?.addEventListener('click',()=>openNav(false));setInterval(()=>{if(paused||images.length<2)return;images[slide].classList.remove('is-active');slide=(slide+1)%images.length;images[slide].classList.add('is-active')},3600);motion?.addEventListener('click',()=>{paused=!paused;motion.setAttribute('aria-pressed',String(paused));motion.textContent=paused?'Play gallery':'Pause gallery'});

